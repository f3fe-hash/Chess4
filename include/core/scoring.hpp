#pragma once

using Evaluation = float;

const inline Evaluation CHECKMATE_SCORE = 1e+5;

// Piece values
const inline Evaluation PAWN_VALUE    = 100;
const inline Evaluation KNIGHT_VALUE  = 300;
const inline Evaluation BISHOP_VALUE  = 320;
const inline Evaluation ROOK_VALUE    = 500;
const inline Evaluation QUEEN_VALUE   = 900;

